<section>
  <div class="ve-nav cf">
    <div class="ve-nav-lists">
      <ul class="breadcrumbs">
        {{#breadcrumbs}}
          {{^is_last}}
            <li>
              <a href="{{url}}">{{#is_first}}<span class="svg-icon-home"></span>{{/is_first}}{{title}}</a>
            </li>
          {{/is_last}}
          {{#is_last}}
            <li>
              <span class="lastitem"><strong>{{title}}</strong></span>
            </li>
          {{/is_last}}
        {{/breadcrumbs}}
      </ul>
    </div>
  </div>
</section>
